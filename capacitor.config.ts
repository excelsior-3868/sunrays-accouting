import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sunrays.accounting',
  appName: 'Sunrays Accounting',
  webDir: 'dist'
};

export default config;
